{
  "name": "pixrity-ar-experience",
  "version": "1.0.0",
  "description": "Augmented Reality for Luxury Retail",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '*.html, *.css, *.js, assets/*'",
    "build": "mkdir -p dist && cp -r index.html index.css index.js assets netlify.toml dist",
    "deploy": "echo 'Connect this directory to Netlify or run netlify deploy'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}